export type NavItem = {
  label: string;
  href: string;
};

export type Contact = {
  name: string;
  role: string;
  email: string;
  topic: string;
  detail: string;
};

export type Faq = {
  id: string;
  question: string;
  answer: string;
};
