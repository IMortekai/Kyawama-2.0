export type NavItem = {
  label: string;
  href: string;
};

export type Contact = {
  name: string;
  role: string;
  email: string;
  topic: string;
};

export type SectionIntro = {
  id: string;
  eyebrow: string;
  heading: string;
};
